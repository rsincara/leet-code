const fn = require('./index');

describe('Tests', () => {
    test('Test 1', () => {
        expect(fn('')).toBe(1);
    });

    test('Test 2', () => {
        expect(fn('')).toBe(1);
    });

    test('Test 3', () => {
        expect(fn('')).toBe(1);
    });
});
