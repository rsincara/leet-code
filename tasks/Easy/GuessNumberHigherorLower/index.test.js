const fn = require('./index');

describe('Tests', () => {
	test('Test 1', () => {
    	expect(fn(10)).toBe(null);
	});

	test('Test 2', () => {
    	expect(fn(6)).toBe(null);
	});

	test('Test 3', () => {
    	expect(fn(1)).toBe(null);
	});

	test('Test 4', () => {
    	expect(fn(1)).toBe(null);
	});

	test('Test 5', () => {
    	expect(fn(2)).toBe(null);
	});

	test('Test 6', () => {
    	expect(fn(1)).toBe(null);
	});
});